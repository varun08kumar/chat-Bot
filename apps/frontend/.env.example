# Base path for API calls. In dev the Vite proxy maps this to the chat and
# metrics services; in production the ingress handles the routing.
VITE_API_BASE=/api

# Dev-only proxy targets (used by vite.config.ts).
VITE_CHAT_TARGET=http://localhost:8001
VITE_METRICS_TARGET=http://localhost:8004
